#!/bin/bash

##
# For this script to work correctly, you should use it when you're at the root of the website with the terminal (e.g., you@you:/path/to/root-website$ bash ../_tools/php-webserver.sh).
# Warning! This is only to test a PHP script, but not to test the software (pH7 CMS).
##

php -S 127.0.0.1:8080

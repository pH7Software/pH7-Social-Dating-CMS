<div class="center">
	<p><img src="{qr_core}" alt="Two-Factor authentication QR code" /></p>
	<div class="bold">{{ LinkCoreForm::display(t('Enable/Disable Two-Factor Authentication'), 'two-factor-auth', 'main', 'setup/' . $mod, array('status' => $is_enabled)) }}</div>
</div>

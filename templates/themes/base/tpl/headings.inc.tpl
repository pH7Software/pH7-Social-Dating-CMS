<div class="center" id="headings">
    {if !empty($h1_title )}
        <h1>{h1_title}</h1>
    {/if}
    {if !empty($h2_title )}
        <h2>{h2_title}</h2>
    {/if}
    {if !empty($h3_title )}
        <h3>{h3_title}</h3>
    {/if}
    {if !empty($h4_title )}
        <h4>{h4_title}</h4>
    {/if}
</div>
